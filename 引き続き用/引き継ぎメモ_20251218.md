# ================================
# 引き継ぎメモ
# ================================

## プロジェクト概要
- Flask + SQLAlchemy のタスク管理アプリ
- 認証（登録 / ログイン / ログアウト）実装済み
- Docker + Nginx で HTTPS 対応
- GitHub Flow（main + feature ブランチ運用）

---

## 現在の作業ブランチ
- ブランチ名:
  feature/flask-blueprint-refactor

- 作業内容:
  Blueprint 化および factory パターン導入による構成整理  
  （将来的なパスワード再設定・メール機能追加を見据えたリファクタ）

---

## 作業方針 / 判断背景
- 当初予定していたメール機能の追加は一旦保留
- 今後の機能追加・保守性向上を考慮し、Blueprint 化を優先
- アプリ全体を factory パターンで構成する方針とした

---

## 本日の対応内容
- Blueprint 化を見据えたディレクトリ構成の整理を実施
- auth / tasks / profile 用ディレクトリを作成
- extensions.py を配置し、拡張機能分離の土台を作成
- config.py を作成（中身の実装は次回対応予定）

---

## 実装済み / 確認済み
- git 周りの整理（branch 作成、前回修正内容を main に push）
- メール送信機能の技術選定（Flask-Mail + MailHog）
- Blueprint 化および factory パターン導入の方針整理

---

## 未対応 / 次にやること
- Blueprint 化の継続（auth / tasks など機能単位でルーティング分割）
- factory パターン（create_app）でのアプリ初期化整理
- config.py の中身実装（環境変数ベースの設定）
- Flask-Mail 導入
- MailHog（ローカル SMTP）でのメール受信確認
- feature ブランチで実装完了後、main へマージ

---

## 起動・確認手順
- Docker 起動:
  docker-compose up --build

- MailHog 確認:
  http://localhost:8025

- HTTPS 確認:
  https://localhost

---

## Git 操作メモ
- ブランチ確認:
  git status
  git branch

- feature → main マージ手順:
  git checkout main
  git pull
  git merge feature/flask-blueprint-refactor
  git push

---

## 注意点
- .env / 証明書は Git 管理外
- main は常に安定状態を維持
- 実装途中のコードを main に入れない

---
