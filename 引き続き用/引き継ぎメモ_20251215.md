# ================================
# 引き継ぎメモ
# ================================

## プロジェクト概要
- Flask + SQLAlchemy のタスク管理アプリ
- 認証（登録 / ログイン / ログアウト）実装済み
- Docker + Nginx で HTTPS 対応
- GitHub Flow（main + feature ブランチ運用）

---

## 現在の作業ブランチ
- ブランチ名:
  feature/password-reset

- 作業内容:
  パスワード再設定機能（メール送付）の実装途中

---

## 実装済み / 確認済み
- git周りの整理（branch作成、前回の修正内容をmainにpush）
- メール送信機能の技術選定（Flask-Mail + MailHog）

---

## 未対応 / 次にやること
- Flask-Mail 導入
- MailHog（ローカルSMTP）で受信確認可能
- feature ブランチで開発中（main には未マージ）

---

## 起動・確認手順
- Docker 起動:
  docker-compose up --build

- MailHog 確認:
  http://localhost:8025

- HTTPS 確認:
  https://localhost

---

## Git 操作メモ
- ブランチ確認:
  git status
  git branch

- feature → main マージ手順:
  git checkout main
  git pull
  git merge feature/password-reset
  git push

---

## 注意点
- .env / 証明書は Git 管理外
- main は常に安定状態を維持
- 実装途中のコードを main に入れない

---
